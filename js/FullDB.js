function initDB(){
    ractiveDB.set({DBisNotLoaded: false});
    ractiveDB.set({DBisLoaded: true});
    console.log('tessdts');
    $( document ).ready(function() {
    var DB = $.csv.toArrays('stats/final_data');
});
}